import type { User } from "@/types";

interface Props{
    user?: User | undefined
}


export default function ProfilePicture({}:Props) {
    return (
        <div>
            
        </div>
    )
}

