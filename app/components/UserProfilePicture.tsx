import type { User } from "@/types";

interface Props{
    user?: User | undefined
}


export default function ProfilePicture({user}:Props) {
    return (
        <div>
            {}
        </div>
    )
}

